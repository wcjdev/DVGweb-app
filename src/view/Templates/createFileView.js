
function createFileView(){


}

document.querySelectorAll("selector").forEach(function(el){

    el.classList.remove('class');
})


document.querySelectorAll("selector").forEach(function(el){

    el.addEventListener('click', nameofFunction);
});

function updateWhatever(event){
if(event !== undefined && event.target.classList.contains('selected')){

    // do something 
}else if (event !== undefined && !event.target.classList.contains('selected')){
    variable = event.target.id;
};
    event.target.classList.add('class');
    


// data binding
// important to track state of the data
function([para], index){el.textContent=para; el.setAttribute('key', index)}
state = {properties}

let container = document.createElement('el');
let subcont = document.createElement('el1');
let subcont1 = document.createElement('el2');
let image = document.createElement('img');
image.setAttribute('src', state.image)


let containerDiv = document.createElement('div');
constainDev.setAttribute('attr', value);
constainDev.textContent="state.property"
let list = document.createElement('ul');
state.createFileView.forEach(function(){
    let list = document.createElement('li'); 
});

if (document.querySelector('selector')){
    domElementCurrently.replaceChild(newEl, dometobereplaced)
}else{
    domElementCurrently.appendChild('new')
}}
// template literal

el.textContent = `some kind of message ${image}`

// simple animation showing and hiding elements

el.classList.add(clas, "va")


